version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: gaming-playtime-tracker/.next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - gaming-playtime-tracker/node_modules/**/* 